# Migrated to %APPDATA%\JSClipping\config.json
# On first launch, values from this file (if present) are copied there.
# Prefer editing settings in the Electron app (npm run dev).

OBS_URL=ws://localhost:4455
OBS_PASSWORD=CHANGE_ME
CLIP_OUTPUT_DIR=C:\\Clips
